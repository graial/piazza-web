<!DOCTYPE html>
<html>
  <%= render 'layouts/head' %>

  <body>
    <%= render "shared/navbar" unless turbo_native_app? %>
    <%= render "shared/flashes" %>
    <main>
      <%= yield %>
    </main>
  </body>
</html>
